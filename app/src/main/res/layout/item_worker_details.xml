<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="8dp"
    android:layout_marginTop="4dp"
    android:layout_marginEnd="8dp"
    android:layout_marginBottom="4dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="4dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Worker Icon -->
        <ImageView
            android:id="@+id/iv_worker_icon"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_person_24"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

        <!-- Worker Name -->
        <TextView
            android:id="@+id/tv_worker_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:textColor="@android:color/black"
            android:textSize="18sp"
            android:textStyle="bold"
            app:layout_constraintStart_toEndOf="@id/iv_worker_icon"
            app:layout_constraintTop_toTopOf="@id/iv_worker_icon"
            app:layout_constraintEnd_toStartOf="@+id/iv_popup_menu"
            tools:text="Ramesh Kumar"/>

        <!-- Worker Profession -->
        <TextView
            android:id="@+id/tv_worker_profession"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            app:layout_constraintStart_toEndOf="@id/iv_worker_icon"
            app:layout_constraintTop_toBottomOf="@id/tv_worker_name"
            app:layout_constraintEnd_toStartOf="@+id/iv_popup_menu"
            tools:text="Electrician"/>

        <!-- Worker Phone Number -->
        <TextView
            android:id="@+id/tv_worker_phone"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginTop="4dp"
            app:layout_constraintStart_toEndOf="@id/iv_worker_icon"
            app:layout_constraintTop_toBottomOf="@id/tv_worker_profession"
            app:layout_constraintEnd_toStartOf="@+id/iv_popup_menu"
            tools:text="123-456-7890"/>

        <!-- Popup Menu for Edit/Delete -->
        <ImageView
            android:id="@+id/iv_popup_menu"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Options"
            android:padding="8dp"
            android:src="@drawable/ic_menu_24"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>